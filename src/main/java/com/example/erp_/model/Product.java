package com.example.erp_.model;

// Shim: estende o modelo canônico para compatibilidade entre pacotes
public class Product extends com.example.erp.model.Product {
    public Product() { super(); }
}
